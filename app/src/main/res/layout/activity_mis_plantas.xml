<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MisPlantas">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbarMispPlantas"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/toolbarTitleMisPlantas"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Mis Plantas"
            android:textSize="24dp"
            android:textColor="@color/accent_dark" />

    </androidx.appcompat.widget.Toolbar>

    <ImageView
        android:id="@+id/MisPlantasImg"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:scaleType="centerCrop"
        android:src="@drawable/hortalizas" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvMisPlantas"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="3"/>

</LinearLayout>